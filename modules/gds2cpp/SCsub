#!/usr/bin/env python
from misc.utility.scons_hints import *

import os

Import("env")

env.add_source_files(env.modules_sources, "*.cpp")

# Whole-program transpiled output (generated by Gds2cppTool.transpile_program):
# ~1005 per-class translation units + gds2cpp_all.cpp (gds2cpp_bind_all registry).
# harness.cpp picks it up via __has_include("wp/gds2cpp_all.h").
if os.path.isfile(os.path.join(str(Dir(".").srcnode()), "wp", "gds2cpp_all.cpp")):
    env.add_source_files(env.modules_sources, "wp/*.cpp")
